query Launches {
  launches(limit: 10, sort: "launch_year", order: "desc") {
    links {
      flickr_images
      article_link
    }
    launch_year
    id
    mission_name
    mission_id
    rocket {
      rocket_name
    }
  }
}
