query Launches ($limit: Int!, $sort: String!, $order: String!) {
  launches(limit: $limit, sort: $sort, order: $order) {
    links {
      mission_patch_small
    }
    id
    mission_name
    rocket {
      rocket_name
    }
    launch_date_local
  }
}
