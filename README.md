## SpaceX (GraphQL API + Next.js) app

For dev (next.js dev server + graphql-ts-apollo codegen):

```bash
npm run dev
```
```bash
npm run codegen
```

Open [http://localhost:3000](http://localhost:3000)